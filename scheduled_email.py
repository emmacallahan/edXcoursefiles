def scheduled_email(scheduled_for):
    today = datetime.now()
    tomorrow = date+timedelta(days=1)
    if today.date() == scheduled_for.date()
    return 'This email is scheduled for today at 10:50.'

